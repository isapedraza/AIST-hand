# Estado 2026-06-16b -- RESULTADO: el fix del drift FUNCIONO (live)

## Resultado (confirmado live, shadow-only, WiLoR)

Las dos runs UDHM (A y B, ckpt stage1_best_total(18):A/b.pt) **arreglan el drift.**
**Ya NO driftean** los "dedos de lado" que persistian desde Run20. Ambos mejores
que el modelo anterior (ckpt 17 / Run20-base).

- **A** (test limpio: lam_dr=0 lam_udhm=1.2, Xin calibrado igual): arregla el drift.
- **B** (Xin-informed: lam_udhm=2.12 + pinch=0.46 + tip_rot=0.03): arregla el drift,
  **se siente ligeramente mas estable** que A.
- Poca diferencia A vs B, pero ambos > anterior.

## Que prueba esto

La descomposicion de rotacion (stage-3 -> termino udhm per-slot en S_k) **resuelve
la abduccion sub-observada** que causaba el drift. Confirma toda la cadena:
- A aislado prueba que el udhm (abduccion descompuesta) ES el fix (atribucion limpia).
- B muestra que sumar pinch/tip_rot (Xin, re-escalados) da un pelin mas de estabilidad.

Validacion offline lo predijo: udhm ~10x mejor que D_R en discriminar abduccion.
Ahora confirmado en vivo: el drift desaparecio.

## Modelo recomendado

**B** por estabilidad ligeramente mejor. A queda como prueba limpia del fix (tesis).

## Siguiente (cuando haya tiempo)

- 3-manos (shadow+leap+barrett) con la misma metrica udhm.
- Per-pair norm (ya implementado, --per_pair_norm) si se quiere apretar cross-robot.
- Subir w_udhm abduccion solo si reaparece algun drift residual (no parece hacer falta).
