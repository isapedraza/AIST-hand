# Estado 2026-06-15c (sesion noche) -- punto de retorno

Save point. Resultado del shadow-only con lambdas calibrados + replanteo conceptual
de la metrica como cross-embodiment. Continua de ESTADO-2026-06-15b.md.

## Resultado del run (shadow-only, lambdas calibrados)

- Ckpt: `stage1_best_total(17).pt`. B=50k, 15k steps, `--force_cross_robot`, percepcion WiLoR.
- Lambdas (commit `de0bac8`): lam_tip=1.0, lam_thumb_tip=1.3, lam_pinch=0.07, lam_tip_rot=0.0, lam_dr=1.4.
- **Live: se siente como Run20.** Un poquito menos preciso (= techo R2=0.74 de la calibracion,
  no bug). Increiblemente bueno, con detalle.
- **Persiste identico a Run20: "algunos dedos se van de lado"** (abduccion). Nunca corregido.

## Que confirma

1. **Calibracion lambda VALIDADA.** R2=0.74 en regresion vs Run20 S_k -> "siente como Run20" live.
   Metrica S_k solida. El "menos preciso" = techo estructural predicho (D_joints no recuperable de tips).
2. **"Dedos de lado" = ESTRUCTURAL, no de lambdas.** Persiste a pesar de recalibrar -> no es peso
   tip/pinch. Mismo hallazgo Run 22.
3. Decision (per ESTADO-b): shadow-only ~= Run20 -> luz verde 3-manos 30-45k steps mismos lambdas.
   PERO la abduccion es la palanca real, no escalar steps.

## El hallazgo grande: por que ESTO ya es cross-embodiment

Run20 usaba `S_k = D_R + 1.2*D_joints + 0.07*D_ahg`. **D_joints comparaba la CADENA COMPLETA**
(MCP/PIP/DIP/TIP, `chain[N,5,4,3]`, 4 posiciones por dedo). Eso lo ataba a single-robot:

- Los joints INTERMEDIOS (MCP, PIP, DIP) dependen de la geometria especifica de cada mano
  (longitudes de falange, colocacion de juntas). Aun normalizado por hand_length, el PIP humano
  y el PIP robot en "el mismo agarre" NO coinciden -> D_joints fuerza correspondencia punto-por-punto
  de intermedios -> el robot imita la geometria osea del humano, no la pose funcional. Sobre-restringe.
- Cross-robot peor: otra mano = distinto numero de falanges / link lengths / sin pinky (Barrett).
  Comparar la cadena entera hornea la cinematica de la mano fuente en el metric.

Lo nuevo (`--force_cross_robot`, `_cross_robot_contrastive`) tiro D_joints y alinea por
**Xin tips + pinch + D_R sobre joints compartidos** -- cantidades UNIVERSALES que toda mano tiene,
normalizadas por hand_length (size-invariant). **Tips = endpoint FUNCIONAL** (donde ocurre el contacto,
lo que importa para agarrar); los intermedios quedan libres de diferir por robot.

**Doble win, verificado:** tiramos D_joints, alineamos solo por tips+pinch+D_R, e IGUAL llegamos a
calidad Run20. O sea (1) la cadena intermedia no era necesaria -- tips bastan; (2) sin la cadena =
sin hornear geometria source = metric embodiment-agnostico.

**Cross-embodiment es propiedad ESTRUCTURAL del metric (ya, por construccion), no algo que se gana live.**
El 3-manos malo previo era undertraining (770cee638), no falla del metric.

## Caveat honesto: el espectro de invarianza (lo que sigue sin resolver)

`hand_length` normaliza ESCALA GLOBAL, no PROPORCIONES relativas entre dedos. El tip-absoluto
todavia carga proporcion (indice largo vs corto -> tip en xyz normalizado distinto a misma flexion).
NO es el mismo problema que D_joints (el tip residual es el objetivo funcional que QUIERES alinear;
los intermedios de D_joints eran ruido espurio sin significado cross-embodiment). Pero no esta 100%
resuelto. Espectro dentro del propio metric:

| Termino | Invarianza | Retiene |
|---|---|---|
| D_R (rotaciones) | total a escala+proporcion | solo angulos, scale-free |
| pinch (dist tip-tip relativas) | a colocacion de muneca | geometria relativa entre dedos |
| tips (xyz absoluto normalizado) | solo escala global | RETIENE proporcion de dedos |

Hipotesis (no probada): el residual proporcional del tip-absoluto es por que robots de proporcion
muy distinta (Barrett 3 dedos) son mas dificiles. Palanca testeable: **subir lam_pinch (relativo,
mas robusto) sobre lam_tip (absoluto)**. Hoy lam_pinch=0.07 vs lam_tip=1.0 (pinch casi apagado).

## Abduccion: diagnostico corregido + reenfoque geometrico

Metric actual = rot6 (`training/losses.py:12` `_sk_w`), NO el euler de Run22. NO hay peso `mcp_abd`
separado: MCP = un solo peso, flexion+abduccion BUNDLEADAS en el geodesico. Abduccion sub-observada:
1. D_R mcp: flexion (rango grande) domina la magnitud -> abduccion se ahoga.
2. D_joints mcp (ya removido): base del dedo casi no se mueve con abduccion -> ciego al spread.
3. Sin termino explicito de abduccion.
(Nota: Xin tips SI cargan algo de abduccion -- el tip se mueve lateral -- pero grueso, no explicito.
 Medir cuanto capturan los tips solos ANTES de afirmar que falta.)

**Replanteo 6D vs descompuesto (NO es trade-off, son usos distintos):**
- Net I/O (lo que se regresa): 6D. Gimbal lock/discontinuidad euler SI duele (Zhou 2019). Mantener.
- Metric S_k (escalar que LEES, no aprendes): descomponer. Gimbal NO muerde (lees no regresas; dedos
  lejos de singularidad; 2 DOF ROM limitado). Usar swing-twist (no euler) -> ejes sin wraparound.
- Mismo dato: 6D -> rot6d_to_matrix (Gram-Schmidt, ya en rotations.py) -> R 3x3 -> cualquier
  descomposicion. "Descomponer 6D" = convertir, no inventar info.

**Insight clave (lo que reenfoca todo): abduccion por GEOMETRIA, no por LABEL.**
FFJ4 de shadow = abduccion por suerte del URDF; rompe en Barrett/Leap/ball-joints. Definicion robusta:
abduccion = swing sobre el eje perpendicular al plano de flexion (normal de palma); flexion = swing
sobre el hinge. Frame-defined, robot-agnostico. El lado humano YA es geometrico: `gamma` sale del
frame del joint (`dong_kinematics.py:378-413`), no es etiqueta. Medir abduccion geometrica = cantidad
UNIVERSAL -> MANTIENE el metric cross-embodiment (leer FFJ4 lo romperia).

## Sabemos los ejes? SI (verificado en datos)

- Humano Dong (`dong_kinematics.py:1052-1065`): MCP = beta(flex)+gamma(abd) escalares nombrados en
  rad; PIP/DIP solo beta. Cache guarda quats[15,4] bundleado pero gamma recuperable.
- Shadow (`joint_names`): FFJ4/MFJ4/RFJ4/LFJ4=abduccion, J3=mcp flex, J2=pip, J1=dip, LFJ5=arco,
  THJ5..1 pulgar. (usar = label-based, solo shadow; para tesis derivar geometrico y VERIFICAR que
  coincide con FFJ4 como sanity check.)

## Experimento propuesto (NO corrido)

Data local: `synthetic_close_hand_shadow_qpos.npz` (29670 qpos22/24 + open),
`valid_robot_poses_shadow_1M_dong.npz` (quats[15,4]/chain/tips), `hograspnet_dong.csv` humano.
Comparar metric de similitud con 3 reps (6D, quat junto, descompuesto swing-twist) sobre:
- Test A (control flexion): close vs open robot. Todas deben separar (sanity).
- Test B (DECISIVO abduccion): poses base clonadas variando SOLO abduccion (swing-twist GEOMETRICO,
  no leer FFJ4). Medir delta-S_k por rep. Prediccion: 6D/quat bundleado se mueve poco; descompuesto
  proporcional. OJO: medir primero cuanta abduccion capturan tips solos.
- Test C (cross-emb): abduccion-humano(gamma) vs abduccion-robot(geometrica) -- descompuesto alinea
  mejor que geodesico?

NO tenemos papers FDM (Bernardin/CrossDex) ni UDHM/OHRA/UniDexTok en repo -- conseguir PDF si vamos
por esos. Refs presentes: SAME, Yan&Lee, Dong, Xin2025, Kipf-VGAE.

## Sesion continuada: papers conseguidos + EDM + per-pair + D_R = señal intermedia

CORRECCION al bloque de arriba: los papers SI los tenemos. UDHM (Fang/UniDexTok),
OHRA (Wei), Cross-Emb-RL (Yuan) movidos a `/home/yareeez/ThesisGCN/literatura/md/`.
Bernardin EDM (hal-01632850) leido en chat.

### UDHM/OHRA validan la idea geometrica
- UDHM: abduccion rota sobre normal palma n_p, flexion sobre eje lateral e_lat. = nuestra idea
  geometrica formalizada. 22-DoF, angulos normalizados por pi (scale+proporcion free). Citar, no re-derivar.
- OHRA: convencion ejes (x=abd, y=flex, z=axial) = formaliza nuestros yamls. Latente de MORFOLOGIA
  (no pose) -> no es nuestra tarea. Descartar para retargeter.
- Edge intacto: ambos tokenizan/condicionan robot<->robot; ninguno alinea humano->robot. Nuestro
  retargeter es distinto. Novedad.

### CONVERGENCIA: D_R YA es la señal intermedia que buscabamos
La idea de "proyectar todo a comun y comparar cadenas enteras" colapsa a comparar ANGULOS (fijar
esqueleto deja solo configuracion). UDHM = version limpia de esa idea. Y eso = **D_R que YA tenemos**:
per-joint, chain-free, configuracion no posicion (sin degeneracion de tip). "Añadir UDHM" NO es rep
nueva ni cadenas enteras -> es DESCOMPONER el geodesico D_R-MCP en [mcp_abd, mcp_flex, pip, dip]
(swing-twist), misma señal lectura mas fina. Cadena canonica COMPLETA = mimicry total (pierde la
ventaja funcional de Xin); cadena canonica SELECTIVA (solo DoF que el tip no fija, ej abduccion) =
lo mejor de ambos.

### CORRECCION diagnostico abduccion: NO es ceguera, es degeneracion + arquitectural
- tip_pos es 3D completo (wrist->TIP), lam_tip=1.0 -> el metric SI VE abduccion via tips. NO es ciego.
- "Dedos de lado" = degeneracion: misma posicion de tip alcanzable con multiples combos (abd,flex) ->
  tip-position invariante al tradeoff abd<->flex -> abduccion SUB-DETERMINADA, no no-observada.
- EVIDENCIA del usuario (refuta "es el dataset/prior"): drift sistematico a un lado, pasa con TODA
  percepcion (mediapipe+wilor), NO ruido. 27B (single latent) NO lo produce pero tampoco mueve dedos
  independientes. Si fuera dataset, saldria en todos. -> drift ATADO al mecanismo per-finger (latentes/
  subespacios por dedo). Es arquitectural, no data ni percepcion. (Causa exacta NO determinada; no teorizar.)

### Bernardin EDM (FDM): relevante pero NO es fix, comparte nuestra degeneracion
- EDM = todas las distancias inter-joint normalizadas por cadena. Retargeter CLASICO (DGP, cuerpo).
- Nuestro pinch = EDM disperso sobre fingertips. Ya hacemos la version chica.
- Paper documenta SOLITO nuestras 2 limitaciones: (1) "cannot compute end-effector orientations"
  (distancias pierden orientacion -> por eso D_R), (2) "inversions for symmetrical postures, two
  joint sets -> similar distance matrix" (= NUESTRA degeneracion). -> FDM puro NO arregla el drift.
- DECISION: NO meter EDM completo (comparte degeneracion, sin orientacion, reintroduce cadena). Foco D_R.

### Per-pair normalization (lo robable de Bernardin) -- PROPUESTA no verificada
Normalizar cada distancia por tau_chain de ESE par (no hand_length global). d_norm in [0,1] = fraccion
de alcance. Mata el residual de proporcion que el global deja (indice largo vs corto -> mismo valor a
misma extension funcional).
- Aplica a terminos de MAGNITUD: tip_pos (BLANCO PRINCIPAL, lam_tip=1.0) + pinch. NO a tip_rot
  (unitario) ni D_R (rotacion) -> ya invariantes.
- Fix tip_pos: v_f = (wrist->tip_f)/tau_f (cadena bone-lengths del dedo) en vez de /hand_length.
  Codigo: losses.py xin_sk_per_finger ~linea 119. Solo cambiar denominador, no meter EDM.
- NO declarar "superior" sin verificar empiricamente cross-robot.

## Proximos pasos (orden sugerido)

1. Escribir script Test B (swing-twist geometrico, medir delta-S por rep). El que decide.
   Sirve doble: confirma degeneracion y es la metrica de descomposicion D_R-MCP.
2. Segun Test B: si abduccion descompuesta gana claro -> descomponer D_R-MCP en [mcp_abd, mcp_flex,
   pip, dip] (NO rep nueva, solo lectura mas fina del D_R existente).
3. Drift "dedos de lado" -> ubicado en mecanismo per-finger (arquitectural). Causa exacta pendiente,
   investigar decoder per-finger SIN teorizar antes de mirar codigo/datos.
4. Per-pair normalization de tip_pos+pinch (opcional, verificar). Blanco = tip_pos.
5. En paralelo / luego: 3-manos 30-45k steps mismos lambdas (luz verde dada).
